---
apiVersion: notification.toolkit.fluxcd.io/v1
kind: Receiver
metadata:
  name: pbs-backstage
  namespace: pbs-backstage
spec:
  type: github
  events:
    - "ping"
    - "push"
  secretRef:
    name: pbs-backstage-webhook-receiver-token
  resources:
    - kind: GitRepository
      name: pbs-backstage
