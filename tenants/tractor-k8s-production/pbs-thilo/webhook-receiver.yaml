---
apiVersion: notification.toolkit.fluxcd.io/v1
kind: Receiver
metadata:
  name: pbs-thilo
  namespace: pbs-thilo
spec:
  type: github
  events:
    - "ping"
    - "push"
  secretRef:
    name: pbs-thilo-webhook-receiver-token
  resources:
    - kind: GitRepository
      name: pbs-thilo
